rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow anyone to read documents
      allow read: if true;
      
      // Allow signed-in users to write documents
      allow write: if request.auth != null;
    }
  }
}
